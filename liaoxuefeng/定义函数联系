#定义函数联系

import math
def quadratic(a,b,c):
    m=b*b-a*a*c
    if m>0:
        x=(-b+math.sqrt(m))/(2*a)
        y=(-b-math.sqrt(m))/(2*a)
        return x,y
    else:
        return 'no answer'
print(quadratic(2,3,1))
print(quadratic(1,3,1))
print(quadratic(1,4,4))
